node_modules/
dist/
.astro/
*.log
.DS_Store
.env
.env.production
.wrangler/
# Search Console / analytics exports — keep out of the public repo
*Performance-on-Search*.zip

# Stray media dropped at repo root — never commit personal images.
# (Real site assets live in public/, which is not ignored.)
/*.jpg
/*.jpeg
/*.png
/*.webp
/*.gif
